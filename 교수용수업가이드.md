---
title: "AI 로봇 시뮬레이션 수업 가이드 (교수용, v1.1)"
date: 2026-09-13
status: draft
---

# [교수용] AI 로봇 시뮬레이션 수업 가이드 — v1.1 (6주차부터)

* **과목명:** 스마트로봇 운용과 활용(싱글 모드) / 피지컬 AI와 로봇 제어(듀얼 모드)
* **대상:** 코딩이 처음이거나 로봇 제어를 처음 접하는 학생 (비전공자 포함)

> ℹ️ **1~5주차는 v1.0 가이드(`v1.0/교수용수업가이드.md`) 그대로 진행하시면 됩니다** — 그 구간 API·동작은 촬영 완료라 바뀌지 않았습니다. 이 문서는 **6주차부터 새로 추가된 부분**만 다룹니다.

---

## 1. v1.0 → v1.1, 뭐가 달라졌나

| 항목 | v1.0 (1~5주차) | v1.1 (6주차~) |
| --- | --- | --- |
| 로봇 여러 대 | `ServingRobot.exe`를 여러 번 실행 + `-port=5556` 인자 | **한 창**에서 우상단 **[로봇 모드]**로 싱글/듀얼 전환 |
| 파이썬 접속 | `ServingRobot(port=5556)` | `ServingRobot(agent=1)` / `ServingRobot(agent=2)` |
| 회전 잠금 | 실행 인자 `-rotationLocked=true` | 우상단 **[회전 잠금]** 토글 버튼 |
| 재시작 | 앱을 껐다 켜야 함 | 좌하단 **[재시작]** 버튼 / **F5** / 좌상단 새로고침 아이콘 |
| 센서 | `scan()` 하나(전방 단일 방향) | `scan()`(유지) + `scan_lidar()`(6방향) + `scan_ultrasonic()` + `is_obstacle_near()`/`get_alert_level()`(전방 초음파류, 사거리 1.5m) |
| 충돌 시 | 항상 프로그램이 그대로 멈춤 | `move()`는 여전히 멈춤 / `move_safe()`는 `CollisionError`로 잡아서 계속 진행 가능 |

> ⚠️ **구버전 방식(`-port=`, `ServingRobot(port=...)`)은 v1.1 앱에서 더 이상 동작하지 않습니다.** 혹시 예전에 만들어둔 예시 코드나 슬라이드가 있다면 이 표를 기준으로 갱신해 주세요.

---

## 2. 수업 준비물 (6주차 시작 전 Checklist)

* ✅ **v1.1 재설치**: 학생들에게 GitHub 릴리스에서 새로 다운로드하도록 안내 (아래 3번 참고). `serving_robot.py`를 새 파일로 **반드시 교체**해야 합니다 — 안 그러면 `AttributeError`가 납니다.
* ✅ **로봇 모드 확인**: 스마트로봇 운용과 활용 = 싱글(기본값), 피지컬 AI와 로봇 제어 = **듀얼로 전환** 필요.
* ✅ **주차별 시뮬레이터 토글 설정** — 수업 시작 전 우상단에서 미리 켜두세요.

| 주차 | 켜둘 토글 |
| --- | --- |
| 6주차 | [회전 잠금] ON (피지컬AI는 + [동적 장애물] ON) |
| 7주차 | [로그 저장] ON |
| 10주차 | [NPC 사람] ON (피지컬AI는 + [동적 장애물] ON) |
| 11주차 | [탐색 맵] ON (원하면) |

---

## 3. v1.1 다운로드 안내 (학생에게 그대로 읽어주면 됨)

```
Windows: https://github.com/Daehyun04225768/ServingBot/releases/download/v1.1.1/ServingBot_Windows_v1.1.1.zip
Mac:     https://github.com/Daehyun04225768/ServingBot/releases/download/v1.1.1/ServingBot_Mac_v1.1.1.zip

위 주소에서 ServingBot v1.1을 다운로드해서 압축까지 풀어줘.
```

압축을 풀면 `serving_robot.py`가 새로 들어있습니다 — 기존 실습 폴더의 같은 파일을 이걸로 덮어쓰라고 안내해주세요. (자세한 스텝은 `v1.1/학생용실습가이드.md` 2장 참고)

> **교수님 팁:** "1~5주차에서 쓰던 `move()`, `turn()`, `scan()`은 그대로 다 됩니다 — 오늘부터 새 기능이 **추가**되는 것뿐이에요."

---

## 4. 주요 교육 포인트 (6주차 이후, 주차별)

파이썬 문법과 신규 함수가 주차마다 짝지어 늘어납니다. 학생에게 "오늘 배우는 문법이 왜 필요한지"를 이 매핑으로 짚어주면 좋습니다.

| 주차 | 파이썬 문법 | 핵심 신규 함수 |
| --- | --- | --- |
| 6주차 | 조건문 if/elif/else | `stop()` `is_obstacle_near()` `get_alert_level()` `move_curve()` `set_speed()` (피지컬AI는 + `scan_lidar()`) |
| 7주차 | 반복문 for/while | `move_repeat()` `patrol()` `sweep()` `scan_until()` `get_event_history()` `get_log()` |
| 9주차 | 함수 정의 def + 콜백 + 튜플 | `get_position()` `get_direction()` `on_collision()` `on_obstacle()` `on_battery_low()` |
| 10주차 | 딕셔너리 + 예외 처리 try/except | `get_camera_view()` `move_safe()` `charge_at_station()` `display()` `speak()` |
| 11주차 | 알고리즘 종합(동선 최적화) | `navigate_auto()` `get_explored_map()` `get_route_distance()` |

**📍 6주차 — "왜 안전한 코드가 필요한가"**
- `is_obstacle_near()`/`get_alert_level()`는 **최대 1.5m까지만** 봅니다(실제 초음파 센서처럼 짧은 거리만 정확히 봄). `scan_lidar()`(피지컬AI 심화)는 6방향, 최대 20m까지 봅니다.
- 시뮬레이터에서 **Ctrl+L**을 누르면 전방 초음파류 센서 범위가 화면에 실시간 선으로 보입니다(초록/노랑/빨강) — "확인한 순간 이후엔 로봇이 눈을 감은 채로 이동한다"는 걸 눈으로 보여주기 좋은 도구입니다. (피지컬AI 심화) 라이다 나머지 5방향은 필요할 때만 **Ctrl+R**로 따로 켤 수 있습니다(하늘색).

**📍 9주차 — "콜백이 왜 필요한가"**
- `on_battery_low(20, 함수)`처럼 "미리 등록해두면 나중에 자동으로 실행된다"는 개념이 처음 나옵니다. 함수를 변수처럼 다른 함수에 넘긴다는 것 자체가 낯설 수 있으니, "경비원한테 문제 생기면 전화하라고 번호 알려두는 것"같은 비유가 도움이 됩니다.

**📍 10주차 — "move()와 move_safe()의 차이"**
- `move()`로 충돌하면 프로그램이 **그대로 멈춥니다**(1~9주차와 동일 — 아직 예외처리를 안 배웠으니까). `move_safe()`는 같은 충돌이 나도 `CollisionError`로 잡아서 다음 코드를 계속 실행할 수 있습니다. 이 차이 자체가 "왜 try/except가 필요한가"의 답입니다.

---

## 5. 자주 묻는 질문 & 해결법 (Q&A, v1.1 추가분)

| 학생의 질문 (증상) | 해결 가이드 |
| --- | --- |
| **"`AttributeError: no attribute 'move_curve'`가 떠요!"** | 💡 v1.0의 옛날 `serving_robot.py`를 그대로 쓰고 있는 겁니다. v1.1 zip 안의 새 `serving_robot.py`로 교체하도록 안내하세요. |
| **"로봇이 움직이다 갑자기 멈추고 프로그램이 끝나요!"** | 💡 `move()` 중 충돌한 정상 동작입니다(1~9주차 방식). 10주차 이후엔 `move_safe()` + `try/except CollisionError`로 바꾸면 계속 진행됩니다. |
| **"(피지컬AI) 2호기가 안 보여요!"** | 💡 우상단 **[로봇 모드]**가 싱글로 돼 있는 경우입니다. 듀얼로 전환하도록 안내하세요. |
| **"회전이 안 먹혀요! `{"error": "ROTATION_LOCKED"}`가 떠요"** | 💡 오늘 실습 의도대로 정상 동작입니다(6주차: 회전 잠금). `move_curve()`로 우회하도록 안내하세요. |
| **"시뮬레이터를 처음 상태로 되돌리고 싶어요"** | 💡 좌하단 **[재시작]** 버튼, 키보드 **F5**, 또는 좌상단 새로고침 아이콘 버튼 — 셋 다 동일하게 모든 로봇을 초기화합니다. 실행 중이던 파이썬 스크립트는 안 꺼지니 재시작 후 다시 실행해야 합니다. |
| **"센서가 지금 뭘 보는지 궁금해해요"** | 💡 시뮬레이터 화면에서 **Ctrl+L**을 누르면 전방 초음파류 센서(초록/노랑/빨강)가, **Ctrl+R**을 누르면 라이다 나머지 5방향(하늘색)이 각각 독립적으로 실시간 선으로 보입니다. 둘은 서로 다른 단축키라 필요한 것만 켜서 볼 수 있습니다. |
| **"충전소인데 `NotAtStationError`가 떠요"** | 💡 충전소 반경 **1m 이내**로 들어와야 `charge_at_station()`이 성공합니다. 좌표를 더 가깝게 이동시키도록 안내하세요. |

v1.0 공통 Q&A(유료 플랜, Connection Refused, 한글 깨짐, Mac Gatekeeper)는 `v1.0/교수용수업가이드.md` 5장 그대로 유효합니다.

---

## 6. 심화 과제 (Fast Learners, 6주차 이후)

* **"장애물 3단계 반응":** "danger면 멈추고, caution이면 속도 절반, safe면 정상 속도로 5바퀴 순찰해줘"
* **(피지컬AI) "협동 순찰":** "1호기와 2호기가 서로 다른 경로로 동시에 순찰하다가 (5,5)에서 만나면 '집결 완료'를 출력해줘"
* **"배터리 자동 복귀":** "배터리가 25% 이하면 하던 일을 멈추고 충전소로 돌아가게 해줘" (`on_battery_low` 콜백 활용)
* **"최단 동선 챌린지":** "이 5개 좌표를 입력 순서 그대로 돌았을 때와, `navigate_auto()`로 최적화했을 때 거리를 비교해서 보여줘"

---

## 7. 전체 UI 컨트롤 목록 (참고용)

| 위치 | 컨트롤 | 기본값 | 관련 주차 |
| --- | --- | --- | --- |
| 우상단 | [로봇 모드] | 싱글 | 전 주차 (과목별 고정) |
| 우상단 | [회전 잠금] | OFF | 6주차 |
| 우상단 | [동적 장애물] | OFF | 6주차 심화(피지컬AI), 10주차 |
| 우상단 | [NPC 사람] | OFF | 10주차 |
| 우상단 | [로그 저장] | OFF | 7주차 |
| 우상단 | [탐색 맵] | OFF | 11주차 |
| 좌하단 | [재시작] 버튼 / F5 | — | 전 주차 |
| 좌상단 | 새로고침 아이콘 버튼 | — | 전 주차 ([재시작]과 동일 기능) |
| 시뮬레이터 화면(단축키) | Ctrl+L (전방 초음파류 센서 시각화) | OFF | 6주차 이후 전체 |
| 시뮬레이터 화면(단축키) | Ctrl+R (라이다 5방향 시각화, 피지컬AI 심화) | OFF | 6주차 이후 전체 |
