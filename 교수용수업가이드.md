---
title: "AI 로봇 시뮬레이션 수업 가이드 (교수용)"
date: 2024-05-23
status: draft
---

# [교수용] AI 로봇 시뮬레이션 수업 가이드

* **과목명:** AI 로보틱스 기초 / 파이썬 프로그래밍 실습
* **대상:** 코딩이 처음이거나 로봇 제어를 처음 접하는 학생

---

## 1. 이 교구는 무엇인가요?

이 프로그램은 학생들이 작성한 파이썬(Python) 코드가 화면 속 3D 로봇(Unity)을 실제로 움직이게 만드는 **"Sim-to-Real"** 교육용 시뮬레이터입니다.

### 학습 목표:

* 내가 짠 코드가 물리적인 동작으로 변환되는 과정을 눈으로 확인합니다.
* 순차적 명령의 논리 구조를 익힙니다.
* 하드웨어 장비 없이도 로봇 제어의 기초를 안전하게 학습합니다.

---

## 2. 수업 준비물 (Checklist)

수업 시작 전, 학생들의 컴퓨터에 다음 환경이 갖춰져 있는지 확인해 주세요.

* ✅ **Python 설치:** (3.8 이상 권장, 설치 시 'Add to PATH' 체크 필수)
* ✅ **코드 에디터:** VS Code (Visual Studio Code) 권장
* ✅ **실습 파일:** `ServingBot.zip` (학생들에게 배포할 압축 파일)

---

## 3. 수업 진행 순서 (Step-by-Step)

### 1단계: 환경 세팅 (5분)

학생들에게 배포된 압축 파일을 풀고, 화면을 좌우로 분할하여 배치하도록 지도해 주세요.

* **왼쪽 화면:** `1_Simulator` 폴더 안의 `ServingRobot.exe` 실행
  *(경로: `C:\Users\sc\Desktop\ServingBot\ServingBot`)* — 로봇이 카페에 서 있는 화면이 나오면, "너희들이 코딩을 하면 이렇게 되는 거야"라고 하면서 로봇이 움직이는 장면을 보여주세요.
* **오른쪽 화면:** VS Code로 `2_Controller` 폴더 안의 `student_mission.py` 열기
  *(경로: `C:\Users\sc\Desktop\ServingBot\ServingBot`)*

> **교수님 팁:** "여러분, 왼쪽은 '로봇의 몸(Body)'이고, 오른쪽은 '로봇의 뇌(Brain)'입니다. 뇌가 명령을 내려야 몸이 움직입니다!"

---

### 2단계: 첫 번째 명령 실행 (Hello Robot!)

VS Code의 `student_mission.py` 상태에서 학생들에게 코드를 수정하지 말고 바로 실행(`F5` 또는 재생 버튼)하게 하세요.

* **정상 동작:** 검은 터미널 창에 글자가 뜨면서, 왼쪽 시뮬레이터의 로봇이 앞으로 조금 움직입니다.
* **성공 포인트:** "와! 움직인다!"라는 흥미 유발이 핵심입니다.

---

### 3단계: 코드 수정 및 실습 (Main Activity)

학생들에게 `student_mission.py` 파일의 맨 아래쪽(`main` 영역)만 수정하도록 안내해 주세요.

#### [실습 예제 1: 정사각형 그리며 제자리로 오기]

`student_mission.py`에 기본으로 들어있는 예제는 `for` 반복문이 아니라, `count` 변수와 `if`문으로 1~4회차를 순서대로 진행하며 사각형을 그리는 방식입니다.

```python
count = 0  # 현재 그린 변의 개수를 저장하는 변수

# [1회차]
if count == 0:
    robot.move(10)
    robot.turn(90)
    count = 1  # 완료 후 카운트 증가

# [2회차]
if count == 1:
    robot.move(12)
    robot.turn(90)
    count = 2

# [3회차]
if count == 2:
    robot.move(10)
    robot.turn(90)
    count = 3

# [4회차]
if count == 3:
    robot.move(12)
    print("사각형 완성!")
    robot.led_on()  # 완성 기념 불빛!
    count = 4
```

> **참고:** `for i in range(4):` 같은 반복문으로 더 짧게 줄여 쓸 수도 있다는 걸 다음 심화 예제에서 자연스럽게 보여줄 수 있습니다 — 기본 예제가 `if`문 나열인 이유(순차 실행을 눈으로 확인시키기 위함)를 학생들에게 짚어주세요.

#### [실습 예제 2: 서빙 시나리오]

* 주방으로 이동 (`move`)
* 도착 알림 (`led_on`)
* 180도 회전 (`turn`)
* 복귀 (`move`)

---

## 4. 주요 교육 포인트 (Teaching Points)

수업 중 학생들에게 설명해주시면 좋은 개념들입니다.

**📍 1) 좌표계와 회전 (Coordinate System)**
- **이동(Move):** 양수(`+`)는 전진, 음수(`-`)는 후진입니다.
- **회전(Turn):** 양수(`+`)는 오른쪽(시계 방향), 음수(`-`)는 왼쪽(반시계 방향)입니다.
- *(학생들이 가장 많이 헷갈려 하는 부분입니다!)*

**📍 2) 통신 (Communication)**
- 파이썬(뇌)에서 `Move:30`이라는 편지(데이터)를 보내면, 유니티(몸)가 그걸 읽고 움직이는 원리입니다. 이를 '소켓 통신'이라고 부릅니다.

**📍 3) 디버깅 (Debugging)**
- 로봇이 이상하게 움직이나요? 터미널 창의 로그(Log)를 확인하세요.
- 예: `Sending: Move:30` → *"나는 명령을 제대로 보냈어."*
- 로봇이 안 움직임 → *"그럼 시뮬레이터가 꺼졌거나 벽에 막힌 거야."*

---

## 5. 자주 묻는 질문 & 해결법 (Q&A)

수업 중 발생할 수 있는 돌발 상황 대처법입니다.

| 학생의 질문 (증상) | 해결 가이드 |
| --- | --- |
| **"Connection Refused 에러가 떠요!"** | 💡 **시뮬레이터가 꺼져 있습니다.**<br>왼쪽 화면의 `ServingRobot.exe`를 먼저 켜고 파이썬을 실행하세요. |
| **"로봇이 벽을 뚫고 지나가요!"** | 💡 **속도가 너무 빠릅니다.**<br>`move(500)`처럼 너무 큰 숫자를 한 번에 넣지 말고, `50`씩 끊어서 이동하게 하세요. |
| **"한글이 깨져서 나와요!"** | 💡 **VS Code 설정 문제입니다.**<br>기능상 문제는 없으니 영어 로그(`Sending...`)만 확인하라고 안심시켜 주세요. |
| **"업데이트 팝업이 떴어요!"** | 💡 **좋은 소식입니다!**<br>개발팀(대현 교수님)에서 기능을 개선했습니다. 'Yes'를 누르면 자동으로 최신 버전이 됩니다. |

---

## 6. 심화 과제 (Fast Learners)

빨리 끝낸 학생들을 위한 도전 과제입니다.

* **"S자 주행 만들기":** 장애물을 피해서 S자로 움직이는 코드를 짜보세요.
* **"왕복 달리기":** `for` 반복문을 사용하여 주방과 홀을 3번 왕복하게 하세요.
* **"사용자 입력 받기":** `input()` 함수를 써서, 키보드로 거리를 입력받아 움직이게 해보세요.

---

## 7. 이후 주차에서 쓸 심화 기능 (참고용)

1주차(사각형 그리기)에서는 안 쓰지만, 뒤 주차 실습에서 활용할 기능들입니다. 미리 알아두시면 좋습니다.

### 📡 센서 — `robot.scan()`

전방 장애물까지 거리(LiDAR 유사) + 주변에 태그(`Detectable`)가 붙은 물체의 이름·좌표·거리를 반환합니다. "빨간 공을 찾아서 이동" 같은 실습(사물 인식)에 사용합니다.

> ⚠️ 사전 준비 필요: Unity Editor에서 `Detectable` 태그를 만들고, 인식시킬 오브젝트(예: 빨간 공)에 지정해야 `scan()`이 뭔가를 찾아줍니다. 아무것도 안 해두면 항상 `DetectedObjects:None`이 나옵니다.

### 📍 좌표 이동 — `robot.move_to(x, z)`

현재 방향과 상관없이 지정한 좌표로 바로 이동합니다(자동으로 그 방향을 보고 이동). "지정된 좌표로 이동해" 같은 실습에 사용합니다.

### 🔋 배터리 — `robot.get_status()` / `robot.charge()`

이동할 때마다 거리에 비례해 배터리가 소모됩니다(기본: 이동거리 1당 0.5%). `get_status()`로 현재 배터리·위치·LED 상태를 확인하고, `charge()`로 100% 충전할 수 있습니다. "배터리 15% 미만이면 충전소로 복귀" 같은 조건부 제어 실습에 사용합니다.

### 🚧 회전 제약 (심화반 전용)

특정 주차(예: "제자리 회전이 안 되는 로봇" 실습)에서는 `Turn` 명령이 의도적으로 거부되도록 만들 수 있습니다. Unity Editor에서 `RobotController`의 `rotationLocked`를 체크하거나, 실행 시 인자로 `ServingRobot.exe -rotationLocked=true`를 주면 됩니다(빌드를 따로 만들 필요 없이 실행 인자만으로 전환 가능). 이 모드에서는 `robot.turn()`이 실패 응답을 받고, 학생은 `move_to()`로 좌표를 재계산해 곡선 경로로 우회하는 법을 배우게 됩니다. 평소엔 꺼둔 채로(기본값) 실행하면 됩니다.

### 🤖 로봇 여러 대 동시 실행 (심화반 전용)

`ServingRobot.exe`를 여러 번 실행하되, 두 번째부터는 `-port=5556`처럼 다른 포트로 띄우면 로봇 여러 대를 동시에 독립적으로 제어하는 실습이 가능합니다. 파이썬에서는 `ServingRobot(port=5556)`처럼 접속할 포트를 지정하면 됩니다.

| 명령어 | 예시 코드 | 설명 |
| --- | --- | --- |
| **주변 살피기** | `robot.scan()` | 장애물 거리 + 인식된 물체 정보 반환 |
| **좌표 이동** | `robot.move_to(3, 5)` | (x:3, z:5)로 바로 이동 |
| **상태 확인** | `robot.get_status()` | 위치·배터리·LED·충돌 정보 반환 |
| **충전** | `robot.charge()` | 배터리 100%로 충전 |
