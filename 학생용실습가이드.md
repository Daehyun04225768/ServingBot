---
title: "AI 서빙 로봇 실습 가이드 (학생용, v1.1)"
date: 2026-09-13
status: draft
---

# [학생용] AI 서빙 로봇 실습 가이드 — v1.1 (6주차부터)

> ℹ️ 1~5주차는 v1.0으로 진행했습니다. **6주차부터는 이 가이드를 기준으로 v1.1을 새로 설치**합니다 — 프로그램(Claude Code)은 이미 설치돼 있으니 "2. 다운로드 및 실행"부터 다시 하면 됩니다.

---

## 1. 필수 프로그램 설치 (이미 했다면 건너뛰기)

* **Claude Code Desktop 앱:** 터미널 없이, 채팅하듯 자연어로 로봇에게 명령을 내릴 수 있게 해주는 AI 에이전트 앱입니다.
  - **Mac:** [다운로드 링크](https://claude.ai/api/desktop/darwin/universal/dmg/latest/redirect) → 설치 파일 실행
  - **Windows:** [다운로드 링크](https://claude.ai/api/desktop/win32/x64/setup/latest/redirect) → 설치 파일 실행
    - ⚠️ Windows는 실습을 하려면 **Git**이 필요합니다. 없다면 [git-scm.com](https://git-scm.com/downloads/win)에서 먼저 설치하세요.
  - 설치 후 앱 실행 → **Anthropic 계정으로 로그인**(Claude 유료 플랜 Pro/Max 등 필요) → 상단 **"Code" 탭** 클릭

---

## 2. v1.1 다운로드 및 실행 (6주차 — 여기부터 새로 설치)

**어떤 과목을 듣는지에 따라 실행 후 설정이 다릅니다** (다운로드 파일 자체는 같습니다):

| 과목 | 시뮬레이터 우상단 [로봇 모드] |
|---|---|
| 스마트로봇 운용과 활용 | **싱글** (기본값 — 따로 안 건드려도 됨) |
| 피지컬 AI와 로봇 제어 | **듀얼**로 전환 (드롭다운/토글에서 선택) |

**Step 1.** 기존 v1.0 실습 폴더를 그대로 써도 되고, 새 폴더를 만들어도 됩니다. Claude Code Desktop 앱의 **Code 탭 → Local → Select folder**에서 그 폴더를 선택하세요.

**Step 2.** 채팅창에 아래처럼 부탁하세요. (자신의 운영체제에 맞는 것 하나만 사용)

- **Windows:**
  ```
  https://github.com/Daehyun04225768/ServingBot/releases/download/v1.1.1/ServingBot_Windows_v1.1.1.zip
  에서 ServingBot v1.1을 다운로드해서 압축까지 풀어줘.
  ```
- **Mac:**
  ```
  https://github.com/Daehyun04225768/ServingBot/releases/download/v1.1.1/ServingBot_Mac_v1.1.1.zip
  에서 ServingBot v1.1을 다운로드해서 압축까지 풀어줘.
  ```

> ℹ️ 로그인 없이 받아지는 공개 파일이라 GitHub 계정도 필요 없습니다.

**Step 3.** 압축이 풀리면 아래와 같은 구조가 생깁니다.

```text
ServingBot_v1.1/
 ├── ServingRobot.exe (Mac은 ServingBot_v1.1.app)  ← 유니티 시뮬레이터 (로봇의 몸)
 ├── serving_robot.py                               ← 로봇 엔진 v1.1 (수정하지 않습니다 — v1.0 파일을 이걸로 교체)
 └── student_mission.py                              ← 실습할 때 Claude Code가 이 파일을 고쳐가며 실행합니다
```

> ⚠️ **v1.0 폴더를 그대로 쓰고 있다면**, 안에 있던 옛날 `serving_robot.py`를 이번에 새로 받은 파일로 **덮어써야** 합니다 — 안 그러면 이번 주차부터 배우는 새 함수(`move_curve`, `scan_lidar`, `get_camera_view` 등)에서 `AttributeError`가 납니다.

**Step 4.** `ServingRobot.exe`(Mac은 `ServingBot_v1.1.app`)를 실행하세요.

**Step 5.** (피지컬 AI와 로봇 제어 수강생만) 시뮬레이터 우상단 **[로봇 모드]**를 **듀얼**로 전환하세요. 2호기가 화면에 나타나면 성공입니다.

**Step 6.** Claude Code 창에 아래처럼 자연어로 부탁해서 연결을 확인하세요.
```
student_mission.py를 실행할 수 있는 상태인지 확인하고, 실행해서 로봇을 앞으로 5만큼 이동시켜줘.
```
> **성공 화면:** `Connected as agent 1`이 뜨고, 유니티 화면 속 로봇이 실제로 움직이면 성공!

---

## 3. 로봇 조종하기 (나만의 미션 만들기)

`student_mission.py`는 **여러분이 직접 열어서 편집할 필요가 없습니다** — Claude Code에게 원하는 동작을 말로 설명하면 알아서 이 파일을 고치고 실행해줍니다.

```
student_mission.py를 고쳐서, 전방 1m 이내에 장애물이 있으면 멈추고, 없으면 앞으로 5씩 이동하게 해줘.
```

(피지컬 AI와 로봇 제어) 듀얼 모드에서는 이렇게 두 로봇을 각각 지정합니다.
```
robot1 = ServingRobot(agent=1)
robot2 = ServingRobot(agent=2)
```

---

## 4. 로봇이 할 수 있는 것 (참고용, 주차별)

### 1~5주차 — 기본 (v1.0부터 계속 사용)

| 명령어 | 예시 코드 | 설명 |
| --- | --- | --- |
| 이동 | `robot.move(5)` | 앞으로 5만큼 (음수는 뒤로) |
| 회전 | `robot.turn(90)` | 오른쪽으로 90도 (음수는 왼쪽) |
| 좌표로 이동 | `robot.move_to(3, 5)` | (x:3, z:5) 좌표로 바로 이동 |
| 불 켜기/끄기 | `robot.led_on()` / `robot.led_off()` | LED 제어 |
| 주변 살피기 | `robot.scan()` | 전방 장애물 거리 + 주변 인식 물체 |
| 상태 확인 | `robot.get_status()` | 위치·방향·배터리·LED·최근 충돌 |
| 충전 | `robot.charge()` | 배터리 100%로 |

### 6주차 — 조건문(if/elif) + 전방 센서

| 명령어 | 반환 | 설명 |
| --- | --- | --- |
| `robot.stop()` | - | 즉시 정지 |
| `robot.wait(초)` | - | 지정 시간 대기 (서버와 통신 없이 그냥 기다림) |
| `robot.set_speed(비율)` | - | 이동 속도 배율(0.1~2.0) |
| `robot.move_curve(x, z)` | - | 회전 없이 곡선 궤적으로 좌표 이동 (회전 잠금 상황에서 사용) |
| `robot.is_obstacle_near(임계값)` | `bool` | 임계값(m) 이내 장애물 감지 시 `True` — **센서 최대 사거리는 1.5m** |
| `robot.get_alert_level()` | `"safe"/"caution"/"danger"` | danger(<1.0m) / caution(1.0~1.5m) / safe(그 밖·미감지) |
| `robot.scan_lidar()` | `dict` | front/front_right/right/rear/left/front_left 6방향 + `detected_objects` (사거리 20m) |
| `robot.scan_ultrasonic()` | `dict` | `{"distance":.., "warning":..}` (사거리 1.5m) |

> 🖥️ **시뮬레이터 화면에서 `Ctrl+L`을 눌러보세요** — 로봇 전방 초음파류 센서가 실시간으로 무엇을 보고 있는지 선으로 보여줍니다 (초록=안전, 노랑=주의, 빨강=위험). 라이다 나머지 5방향(front_right/right/rear/left/front_left)이 궁금하면 `Ctrl+R`을 따로 눌러보세요 — 하늘색 선으로 보입니다. 둘은 독립적인 단축키라 필요한 것만 켜서 볼 수 있습니다(`scan_lidar()`가 반환하는 `front` 값 자체는 그대로 있고, 화면에 겹쳐 그리지만 않을 뿐입니다).

### 7주차 — 반복문(for/while) + 리스트

| 명령어 | 반환 | 설명 |
| --- | --- | --- |
| `robot.move(거리, speed=배율)` | - | 이번 이동에만 속도 적용 |
| `robot.move_repeat(거리, 횟수)` | - | 거리만큼 지정 횟수 반복 이동 |
| `robot.patrol([(x,z), ...])` | - | 좌표 리스트 순서대로 순찰 |
| `robot.sweep(시작각, 끝각, step)` | `list` | 각도 범위를 훑으며 방향별 거리 반환 |
| `robot.scan_until(조건함수, interval=2)` | `dict` | 조건 충족까지 반복 재스캔 (while 패턴) |
| `robot.get_event_history(limit=10)` | `list[dict]` | 최근 이벤트 기록 |
| `robot.get_log()` | `list[dict]` | 전체 세션 로그 |

### 9주차 — 함수 정의(def) + 콜백 + 튜플

| 명령어 | 반환 | 설명 |
| --- | --- | --- |
| `robot.get_position()` | `(x, z)` | 현재 좌표 (튜플) |
| `robot.get_direction()` | `"N"/"E"/"S"/"W"` | 현재 방향 |
| `robot.is_moving` | `bool` | 이동 중 여부 (괄호 없는 속성) |
| `robot.on_collision(콜백함수)` | - | 충돌 시 콜백 자동 실행 |
| `robot.on_obstacle(임계값, 콜백함수)` | - | 임계값 이내 접근 시 콜백 자동 실행 |
| `robot.on_battery_low(퍼센트, 콜백함수)` | - | 배터리가 그 이하로 떨어지면 콜백 자동 실행 |
| `robot.name` | `str` | 로봇 이름 (`"robot1"` 등) |

```python
def 배터리_경고():
    print(f"{robot.name} 배터리 부족!")

robot.on_battery_low(20, 배터리_경고)
```

### 10주차 — 딕셔너리 + 예외 처리(try/except)

| 명령어 | 반환 | 설명 |
| --- | --- | --- |
| `robot.get_camera_view()` | `dict` | `{"camera_view":[{"name","type","distance","angle"}], "obstacle_distance"}` |
| `robot.get_nearby_objects()` | `list[dict]` | 주변 감지 오브젝트 목록 |
| `robot.find_object(이름)` | `dict` or `None` | 이름으로 특정 오브젝트 조회 |
| `robot.move_safe(거리)` | - | 충돌 시 크래시 대신 `CollisionError` 발생 |
| `robot.charge_at_station()` | - | 충전소 밖이면 `NotAtStationError` 발생 |
| `robot.display(문구)` | - | 말풍선 텍스트 표시 |
| `robot.speak(문구)` | - | 말풍선 + (실물 연동 시 음성) |
| `robot.play_sound(클립)` | - | `"beep"`/`"alert"`/`"done"` |

```python
try:
    robot.move_safe(10)
except CollisionError:
    robot.stop()
    robot.speak("충돌 감지! 후진합니다.")
    robot.move(-3)
```

### 11주차 — SLAM + 동선 최적화

| 명령어 | 반환 | 설명 |
| --- | --- | --- |
| `robot.get_explored_map()` | `dict` | 지금까지 지나간/장애물 칸 좌표 |
| `robot.navigate_auto([(x,z), ...])` | `list` | 최단 동선으로 재정렬 후 자동 순찰 |
| `robot.get_route_distance([(x,z), ...])` | `float` | 순서대로 방문 시 총 이동 거리 |

---

## 5. 예외 3종 정리 (10주차 이후 자주 만납니다)

| 예외 | 언제 발생 | 대응 |
| --- | --- | --- |
| `CollisionError` | `move_safe()` 이동 중 충돌 | `except CollisionError:`로 잡아서 후진·우회 |
| `NotAtStationError` | 충전소 밖에서 `charge_at_station()` | 충전소로 이동 후 재시도 |
| `RotationLockedError` | [회전 잠금] 켜진 상태에서 `turn()` | `move_curve()`로 회전 없이 우회 |

> ℹ️ `move()`(안전하지 않은 버전)로 이동하다 충돌하면 **예외 없이 프로그램이 그대로 멈춥니다**(1~9주차 방식). `move_safe()`를 써야 `except`로 잡아서 이어갈 수 있습니다.

---

## 6. 문제 해결 (Troubleshooting)

**🚨 Claude Code가 "유료 플랜이 필요하다"고 해요!**
- Pro/Max 등 유료 플랜 계정으로 로그인하세요.

**🚨 "ServingBot에 연결할 수 없다"고 해요! (Connection Refused)**
- 시뮬레이터(`ServingRobot.exe`/`.app`)를 먼저 실행하고 다시 부탁하세요.

**🚨 `AttributeError: 'ServingRobot' object has no attribute 'move_curve'` 같은 에러가 나요!**
- v1.0의 옛날 `serving_robot.py`를 그대로 쓰고 있는 경우입니다. "2. v1.1 다운로드 및 실행"을 다시 따라가서 `serving_robot.py`를 새 파일로 교체하세요.

**🚨 로봇이 움직이다가 그냥 멈추고 프로그램이 끝나버려요!**
- `move()`로 이동 중 충돌한 겁니다(정상 동작, 1~9주차 방식). 10주차 이후엔 `move_safe()` + `try/except CollisionError`로 바꿔서 계속 진행하게 만들 수 있습니다.

**🚨 시뮬레이터를 처음 상태로 되돌리고 싶어요!**
- 시뮬레이터 좌하단 **[재시작]** 버튼을 누르거나 키보드 **F5**를 누르세요(좌상단의 새로고침 아이콘 버튼도 같은 기능입니다). 실행 중이던 파이썬 스크립트는 안 꺼지니, 재시작 후 스크립트를 다시 실행하세요.

**🚨 로봇 앞 센서가 지금 뭘 보고 있는지 궁금해요!**
- 시뮬레이터 화면에서 **Ctrl+L**을 누르면 전방 초음파류 센서(초록/노랑/빨강 선)가, **Ctrl+R**을 누르면 라이다 나머지 5방향(하늘색 선)이 각각 독립적으로 실시간으로 보입니다.

**🚨 (Mac) 실행하려니 "확인할 수 없는 개발자"라고 막아요!**
- 앱을 우클릭(또는 Control+클릭) → 열기 → 다시 뜨는 창에서 열기.

**🚨 (듀얼 모드) 2호기가 안 보여요!**
- 시뮬레이터 우상단 **[로봇 모드]**가 싱글로 돼 있는지 확인하고 듀얼로 바꾸세요.
