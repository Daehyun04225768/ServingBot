---
title: "AI 서빙 로봇 실습 가이드 (학생용)"
date: 2024-05-23
status: draft
---

# [학생용] AI 서빙 로봇 실습 가이드

환영합니다! 여러분은 코드를 직접 짜지 않고, **Claude Code에게 말로 부탁**해서 가상 환경의 서빙 로봇을 조종하게 됩니다.

---

## 1. 필수 프로그램 설치

실습을 위해 아래 프로그램을 설치해주세요. (이미 설치되어 있다면 패스!)

* **Claude Code:** 터미널에서 자연어로 로봇에게 명령을 내릴 수 있게 해주는 AI 에이전트입니다.
  - **Mac** — 터미널(Terminal) 앱을 열고:
    ```bash
    curl -fsSL https://claude.ai/install.sh | bash
    ```
  - **Windows** — PowerShell을 열고:
    ```powershell
    irm https://claude.ai/install.ps1 | iex
    ```
  - 설치 후 같은 창에 `claude`라고 입력 → 브라우저 로그인 창이 뜨면 **Claude 유료 플랜(Pro/Max 등) 계정**으로 로그인하세요. (무료 claude.ai 계정으로는 안 됩니다.)

> ℹ️ Python도 내부적으로 필요하지만, 따로 설치하지 않아도 됩니다 — 실습 3에서 Claude Code에게 부탁하면 알아서 확인·설치해줍니다.

---

## 2. 다운로드 및 실행 (가장 중요!)

**다운로드도 Claude Code에게 시킵니다.** 브라우저에서 GitHub 페이지를 찾아 들어갈 필요 없습니다.

**Step 1.** 실습용 폴더를 하나 만들고(예: `ServingBot실습`), 그 폴더에서 터미널을 열고 `claude`라고 입력해 Claude Code를 실행하세요.

**Step 2.** Claude Code에게 아래처럼 부탁하세요. (자신의 운영체제에 맞는 것 하나만 사용)

- **Windows:**
  ```
  https://github.com/Daehyun04225768/ServingBot/releases/latest/download/ServingBot_Windows_v1.0.0.zip
  에서 ServingBot을 다운로드해서 압축까지 풀어줘.
  ```
- **Mac:**
  ```
  https://github.com/Daehyun04225768/ServingBot/releases/latest/download/ServingBot_Mac_v1.0.0.zip
  에서 ServingBot을 다운로드해서 압축까지 풀어줘.
  ```

> ℹ️ Claude Code가 터미널에서 직접 다운로드·압축 해제 명령을 실행합니다. 로그인 없이 받아지는 공개 파일이라 GitHub 계정도 필요 없습니다.

**Step 3.** 압축이 풀리면 아래와 같은 구조가 생깁니다.

```text
ServingBot_v1.0/
 ├── ServingRobot.exe (Mac은 .app)   ← 유니티 시뮬레이터 (로봇의 몸)
 ├── serving_robot.py                ← 로봇 엔진 (Claude Code도 이 파일은 수정하지 않습니다)
 └── student_mission.py              ← 실습할 때 Claude Code가 이 파일을 고쳐가며 실행합니다
```

**Step 4.** `ServingRobot.exe`(Mac은 `ServingBot_v1.0.app`)를 실행하세요. (로봇이 카페에 서 있는 화면이 나옵니다.)

**Step 5.** Claude Code 창에 아래처럼 자연어로 부탁하세요.
```
student_mission.py를 실행할 수 있는 상태인지 확인하고, 실행해서 로봇을 앞으로 10만큼 이동시켜줘.
```

> **성공 화면:** 터미널에 `Connected to Unity Simulator...`가 뜨고, 유니티 화면 속 로봇이 실제로 움직이면 성공!

---

## 3. 로봇 조종하기 (나만의 미션 만들기)

`student_mission.py` 파일 안에는 이미 "사각형 그리기" 예제가 들어있습니다. **여러분은 이 파일을 직접 열어서 편집할 필요가 없습니다** — Claude Code에게 원하는 동작을 말로 설명하면, Claude Code가 이 파일을 알아서 고치고 실행해줍니다.

예를 들어 이렇게 부탁해보세요:

```
student_mission.py를 고쳐서, 로봇이 정삼각형을 그리도록 만들어줘. 한 변은 15로 해줘.
```

```
로봇을 앞으로 20 이동시키고, LED를 켜고, 180도 돌려줘.
```

> ℹ️ 참고로 기본 예제는 `count`라는 변수로 몇 번째 변을 그리는 중인지 기억하면서, 1~4회차에 걸쳐 사각형을 그리는 코드입니다. Claude Code에게 "지금 student_mission.py에 어떤 코드가 있는지 보여줘"라고 하면 내용을 보여줍니다.

---

## 4. 로봇이 할 수 있는 것 (참고용)

아래는 Claude Code가 여러분의 자연어 요청을 처리할 때 내부적으로 사용하는 명령어입니다. 여러분이 직접 코드를 쓸 일은 없지만, "로봇이 뭘 할 수 있는지" 알아두면 Claude Code에게 더 구체적으로 부탁할 수 있습니다.

| 명령어 | 예시 코드 | 설명 |
| --- | --- | --- |
| **이동** | `robot.move(30)` | 앞으로 30만큼 갑니다. (음수는 뒤로) |
| **회전** | `robot.turn(90)` | 오른쪽으로 90도 돕니다. (음수는 왼쪽) |
| **불 켜기** | `robot.led_on()` | 로봇의 LED를 켭니다. |
| **불 끄기** | `robot.led_off()` | 로봇의 LED를 끕니다. |
| **좌표로 이동** | `robot.move_to(3, 5)` | 지금 방향과 상관없이 (x:3, z:5) 좌표로 바로 이동합니다. |
| **주변 살피기** | `robot.scan()` | 앞쪽 장애물까지 거리와, 주변에 인식되는 물체(이름·좌표·거리)를 알려줍니다. |
| **상태 확인** | `robot.get_status()` | 현재 위치·방향·배터리·LED·최근 충돌 정보를 한 번에 알려줍니다. |
| **충전** | `robot.charge()` | 배터리를 100%로 채웁니다. |
| **직접 명령** | `robot.send_command("Move:50")` | 문자열로 직접 명령합니다. |

> ℹ️ **이동(move/turn)과 다르게, `scan()`과 `get_status()`는 결과를 돌려줍니다(return).** 아래처럼 변수에 담아서 활용할 수 있습니다.
>
> ```python
> result = robot.scan()
> print("스캔 결과:", result)
> # 예: [ROBOT ACK] [SCAN] ObstacleDistance:None | DetectedObjects:RedBall(x:0.0,z:5.0,dist:7.3)
> ```

---

## 5. 문제 해결 (Troubleshooting)

**🚨 Claude Code가 "유료 플랜이 필요하다"고 해요!**
- **이유:** 무료 claude.ai 계정으로는 Claude Code를 쓸 수 없습니다.
- **해결:** Pro/Max 등 유료 플랜 계정으로 로그인하세요.

**🚨 Claude Code가 "ServingBot에 연결할 수 없다"고 해요! (Connection Refused)**
- **이유:** 시뮬레이터(유니티)가 꺼져 있어서 갈 곳이 없는 상태입니다.
- **해결:** `ServingRobot.exe`(Mac은 `.app`)를 먼저 실행하고 Claude Code에게 다시 부탁하세요.

**🚨 로봇이 움직이다가 멈췄어요!**
- **이유:** 벽이나 장애물에 부딪히면 `[SYSTEM ALERT]`가 뜨고 로봇이 안전을 위해 멈춥니다.
- **해결:** 시뮬레이터를 껐다 켜거나, "충돌 안 하게 조심해서 이동해줘"라고 Claude Code에게 부탁하세요.

**🚨 터미널에 한글이 깨져서 나와요!**
- **해결:** 터미널 인코딩 설정 문제입니다. 동작에는 문제없으니 안심하세요.

**🚨 (Mac) 실행하려니 "확인할 수 없는 개발자"라고 막아요!**
- **해결:** 앱을 우클릭(또는 Control+클릭) → 열기 → 다시 뜨는 창에서 열기. 그래도 안 되면 Claude Code에게 "ServingBot 앱이 실행이 안 되는데 확인해줘"라고 부탁하세요.
