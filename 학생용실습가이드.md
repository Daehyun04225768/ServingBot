---
title: "AI 서빙 로봇 실습 가이드 (학생용)"
date: 2024-05-23
status: draft
---

# [학생용] AI 서빙 로봇 실습 가이드

환영합니다! 여러분은 이제 파이썬 코드를 사용하여 가상 환경의 서빙 로봇을 직접 조종하게 됩니다.

---

## 1. 필수 프로그램 설치

실습을 위해 아래 두 가지 프로그램을 설치해주세요. (이미 설치되어 있다면 패스!)

* **Python 3.x:** 설치 링크 (설치할 때 **'Add Python to PATH'** 체크 필수! ✅)
* **VS Code:** 설치 링크

---

## 2. 다운로드 및 실행 (가장 중요!)

교수님이 공유해주신 GitHub 링크에서 파일을 다운로드합니다. 이번에는 알집으로 제공합니다.

### 📁 폴더 구조를 이렇게 만드세요:

```text
내_프로젝트/
 ├── 1_Simulator/          (유니티 시뮬레이터 압축 푼 것)
 │    └── ServingRobot.exe
 └── 2_Controller/         (파이썬 코드)
      ├── serving_robot.py    (로봇 엔진 — 수정하지 않습니다)
      └── student_mission.py  (여러분이 수정·실행할 파일)
```

### 🚀 실행 순서 (반드시 지켜주세요!)

1. 먼저 `1_Simulator` 폴더의 `ServingRobot.exe`를 실행하세요. (로봇이 카페에 서 있는 화면이 나옵니다.)
2. 그다음 VS Code를 켜고 **`student_mission.py`** 파일을 엽니다. (`serving_robot.py`는 로봇을 움직이는 엔진 파일이라 건드리지 않아도 됩니다.)
3. VS Code 오른쪽 위의 재생 버튼(▶)을 누르거나 `F5`를 누릅니다.

> **성공 화면:** 아래쪽 터미널에 `✅ Connected to Unity Simulator...`라고 뜨면 연결 성공!

---

## 3. 코딩 실습하기 (나만의 로봇 만들기)

`student_mission.py` 파일을 열면 이미 아래와 같은 예제 코드가 들어있습니다. `# 👇 여기서부터 자유롭게 명령을 내려보세요!` 아래쪽이 여러분이 수정할 곳입니다.

```python
from serving_robot import ServingRobot
import time

robot = ServingRobot()
print("\n=== 🚀 로봇 조종 실습을 시작합니다! ===\n")

try:
    # 👇 여기서부터 자유롭게 명령을 내려보세요!

    count = 0  # 현재 그린 변의 개수를 저장하는 변수

    # [1회차]
    if count == 0:
        robot.move(10)
        robot.turn(90)
        count = 1  # 완료 후 카운트 증가

    # [2회차]
    if count == 1:
        robot.move(12)
        robot.turn(90)
        count = 2

    # [3회차]
    if count == 2:
        robot.move(10)
        robot.turn(90)
        count = 3

    # [4회차]
    if count == 3:
        robot.move(12)
        print("사각형 완성!")
        robot.led_on()  # 완성 기념 불빛!
        count = 4

except Exception as e:
    print(f"\n[오류 발생] 로봇 조종 중 문제가 발생했습니다: {e}")

finally:
    robot.close()
    print("\n=== 🏁 실습이 종료되었습니다. ===")
```

> 이 예제는 `count` 변수로 몇 번째 변을 그리는 중인지 기억하면서, 1~4회차에 걸쳐 사각형을 그립니다. 마지막 4회차까지 마치면 `led_on()`으로 완성을 알립니다.

---

## 4. 자주 쓰는 명령어 목록 (치트키)

| 명령어 | 예시 코드 | 설명 |
| --- | --- | --- |
| **이동** | `robot.move(30)` | 앞으로 30만큼 갑니다. (음수는 뒤로) |
| **회전** | `robot.turn(90)` | 오른쪽으로 90도 돕니다. (음수는 왼쪽) |
| **불 켜기** | `robot.led_on()` | 로봇의 LED를 켭니다. |
| **불 끄기** | `robot.led_off()` | 로봇의 LED를 끕니다. |
| **좌표로 이동** | `robot.move_to(3, 5)` | 지금 방향과 상관없이 (x:3, z:5) 좌표로 바로 이동합니다. |
| **주변 살피기** | `robot.scan()` | 앞쪽 장애물까지 거리와, 주변에 인식되는 물체(이름·좌표·거리)를 알려줍니다. |
| **상태 확인** | `robot.get_status()` | 현재 위치·방향·배터리·LED·최근 충돌 정보를 한 번에 알려줍니다. |
| **충전** | `robot.charge()` | 배터리를 100%로 채웁니다. |
| **직접 명령** | `robot.send_command("Move:50")` | 문자열로 직접 명령합니다. |

> ℹ️ **이동(move/turn)과 다르게, `scan()`과 `get_status()`는 결과를 돌려줍니다(return).** 아래처럼 변수에 담아서 활용할 수 있습니다.
>
> ```python
> result = robot.scan()
> print("스캔 결과:", result)
> # 예: [ROBOT ACK] [SCAN] ObstacleDistance:None | DetectedObjects:RedBall(x:0.0,z:5.0,dist:7.3)
> ```

---

## 5. 문제 해결 (Troubleshooting)

**🚨 "Connection Refused" 에러가 떠요!**
- **이유:** 시뮬레이터(유니티)가 꺼져 있어서 갈 곳이 없는 상태입니다.
- **해결:** `ServingRobot.exe`를 먼저 실행하고 다시 코드를 돌려보세요.

**🚨 로봇이 움직이다가 멈췄어요!**
- **이유:** 벽이나 장애물에 부딪히면 `[SYSTEM ALERT]`가 뜨고 로봇이 안전을 위해 멈춥니다.
- **해결:** 시뮬레이터를 껐다 켜거나, 충돌하지 않게 거리를 조절하세요.

**🚨 검은 창(터미널)에 글씨가 깨져요!**
- **해결:** VS Code 터미널 설정이 한글을 지원하지 않아서 그렇습니다. 동작에는 문제없으니 안심하세요! (영어 로그를 권장합니다)
